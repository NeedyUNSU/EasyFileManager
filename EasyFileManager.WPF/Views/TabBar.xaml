<UserControl x:Class="EasyFileManager.WPF.Views.TabBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:EasyFileManager.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="40" 
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:TabBarViewModel, IsDesignTimeCreatable=False}">

    <Border x:Name="RootBorder"
            Background="{DynamicResource MaterialDesignToolBarBackground}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="0,0,0,1">
        <DockPanel>
            <!-- Add Tab Button (Right side) -->
            <Button DockPanel.Dock="Right"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Command="{Binding NewTabCommand}"
                    ToolTip="New Tab (Ctrl+T)"
                    Width="32"
                    Height="32"
                    Margin="4,0">
                <materialDesign:PackIcon Kind="Plus" Width="18" Height="18"/>
            </Button>

            <!-- Tabs ScrollViewer -->
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled"
                          Margin="4,0">
                <ItemsControl ItemsSource="{Binding Tabs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Single Tab -->
                            <Border x:Name="TabBorder"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="4,4,0,0"
                                    Margin="2,4,2,0"
                                    Padding="8,4"
                                    MinWidth="120"
                                    MaxWidth="200"
                                    Cursor="Hand"
                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    PreviewMouseLeftButtonDown="Border_PreviewMouseLeftButtonDown">

                                <Border.InputBindings>
                                    <MouseBinding MouseAction="MiddleClick"
                                                  Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  CommandParameter="{Binding}"/>
                                </Border.InputBindings>

                                <Border.ContextMenu>
                                    <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                        <!-- Note: PlacementTarget.Tag will be set to TabBarViewModel via code-behind -->

                                        <MenuItem Header="New Tab"
                                                  Command="{Binding NewTabInCurrentDirectoryCommand}">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="Plus"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <MenuItem Header="Duplicate Tab"
                                                  Command="{Binding DuplicateActiveTabCommand}">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="ContentDuplicate"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <Separator/>

                                        <MenuItem Command="{Binding TogglePinCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Header>
                                                <TextBlock Text="{Binding PlacementTarget.DataContext.IsPinned, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BooleanToPinTextConverter}}"/>
                                            </MenuItem.Header>
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="Pin"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <Separator/>

                                        <MenuItem Header="Close"
                                                  Command="{Binding CloseTabCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="Close"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <MenuItem Header="Close Other Tabs"
                                                  Command="{Binding CloseOtherTabsCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="CloseBoxMultiple"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <MenuItem Header="Close Tabs to the Right"
                                                  Command="{Binding CloseTabsToRightCommand}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="ChevronRight"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <DockPanel>
                                    <!-- Close Button -->
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Width="20"
                                            Height="20"
                                            Padding="0"
                                            Margin="4,0,0,0"
                                            Opacity="0.7">
                                        <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                                    </Button>

                                    <!-- Pin Icon (if pinned) -->
                                    <materialDesign:PackIcon DockPanel.Dock="Left"
                                                             Kind="Pin"
                                                             Width="14"
                                                             Height="14"
                                                             VerticalAlignment="Center"
                                                             Margin="0,0,6,0"
                                                             Foreground="{DynamicResource SecondaryHueMidBrush}"
                                                             Visibility="{Binding IsPinned, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <!-- Folder Icon -->
                                    <materialDesign:PackIcon DockPanel.Dock="Left"
                                                             Kind="Folder"
                                                             Width="16"
                                                             Height="16"
                                                             VerticalAlignment="Center"
                                                             Margin="0,0,6,0"
                                                             Opacity="0.7"/>

                                    <!-- Tab Title -->
                                    <TextBlock Text="{Binding DisplayTitle}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding ToolTip}"
                                               FontSize="12"/>
                                </DockPanel>
                            </Border>

                            <!-- DataTriggers for Active State -->
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                                    <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                </DataTrigger>

                                <Trigger SourceName="TabBorder" Property="IsMouseOver" Value="True">
                                    <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource MaterialDesignDivider}"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
