<Window x:Class="EasyFileManager.WPF.Views.ExtractArchiveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Extract Archive" 
        Height="400" 
        Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}">
    <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}"
            BorderThickness="2"
            CornerRadius="4">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="FolderZipOutline" 
                                         Width="32" 
                                         Height="32"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                <TextBlock Text="Extract Files"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Info -->
            <Border Grid.Row="1"
                    Background="{DynamicResource MaterialDesignCardBackground}"
                    Padding="12"
                    CornerRadius="4"
                    Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Opacity="0.7"
                               Margin="0,0,0,4">
                        <Run Text="Extracting"/>
                        <Run x:Name="ItemCountRun" Text="5 items" FontWeight="SemiBold"/>
                        <Run Text="from"/>
                    </TextBlock>
                    <TextBlock x:Name="ArchiveNameTextBlock"
                               Text="archive.zip"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Destination Path -->
            <DockPanel Grid.Row="2" Margin="0,0,0,16">
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Click="BrowseButton_Click"
                        ToolTip="Browse..."
                        Margin="8,0,0,0">
                    <materialDesign:PackIcon Kind="FolderOpen"/>
                </Button>

                <TextBox x:Name="DestinationPathTextBox"
                         materialDesign:HintAssist.Hint="Destination folder"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         Text="C:\Extracted"/>
            </DockPanel>

            <!-- Options -->
            <StackPanel Grid.Row="3" Margin="0,0,0,16">
                <TextBlock Text="Options"
                           Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                           Margin="0,0,0,8"/>

                <CheckBox x:Name="PreserveStructureCheckBox"
                          Content="Preserve folder structure"
                          Style="{StaticResource MaterialDesignCheckBox}"
                          IsChecked="True"
                          Margin="0,0,0,8"/>

                <CheckBox x:Name="OverwriteCheckBox"
                          Content="Overwrite existing files"
                          Style="{StaticResource MaterialDesignCheckBox}"
                          Margin="0,0,0,8"/>

                <CheckBox x:Name="OpenFolderCheckBox"
                          Content="Open destination folder after extraction"
                          Style="{StaticResource MaterialDesignCheckBox}"
                          IsChecked="True"/>
            </StackPanel>

            <!-- Spacer -->
            <Grid Grid.Row="4"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="5" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="CANCEL"
                        IsCancel="True"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Click="CancelButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="ExtractButton"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="ExtractButton_Click">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TrayArrowDown"
                                                     Width="18"
                                                     Height="18"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0"/>
                            <TextBlock Text="EXTRACT" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
