<Window x:Class="EasyFileManager.WPF.Views.EditBookmarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Edit Bookmark" 
        Height="320" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}">
    <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}"
            BorderThickness="2"
            CornerRadius="4">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="Pencil" 
                                         Width="32" 
                                         Height="32"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                <TextBlock Text="Edit Bookmark"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Name -->
            <TextBox Grid.Row="1"
                     x:Name="NameTextBox"
                     materialDesign:HintAssist.Hint="Bookmark Name"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Margin="0,0,0,16">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ElementName=SaveButton, Path=Command}"/>
                </TextBox.InputBindings>
            </TextBox>

            <!-- Path -->
            <TextBox Grid.Row="2"
                     x:Name="PathTextBox"
                     materialDesign:HintAssist.Hint="Path"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Margin="0,0,0,16"/>

            <!-- Icon Picker -->
            <ComboBox Grid.Row="3"
                      x:Name="IconComboBox"
                      materialDesign:HintAssist.Hint="Icon"
                      Style="{StaticResource MaterialDesignFilledComboBox}"
                      Margin="0,0,0,24">
                <ComboBoxItem Content="Folder" Tag="Folder"/>
                <ComboBoxItem Content="Home" Tag="Home"/>
                <ComboBoxItem Content="Desktop" Tag="Monitor"/>
                <ComboBoxItem Content="Documents" Tag="FileDocument"/>
                <ComboBoxItem Content="Pictures" Tag="Image"/>
                <ComboBoxItem Content="Music" Tag="Music"/>
                <ComboBoxItem Content="Videos" Tag="Video"/>
                <ComboBoxItem Content="Hard Drive" Tag="Harddisk"/>
                <ComboBoxItem Content="Cloud" Tag="Cloud"/>
                <ComboBoxItem Content="Star" Tag="Star"/>
            </ComboBox>

            <!-- Buttons -->
            <StackPanel Grid.Row="4" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="CANCEL"
                        IsCancel="True"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="SaveButton"
                        Content="SAVE"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
