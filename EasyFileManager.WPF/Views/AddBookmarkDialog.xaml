<Window x:Class="EasyFileManager.WPF.Views.AddBookmarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add Bookmark" 
        Height="260" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}">
    <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}"
            BorderThickness="2"
            CornerRadius="4">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="BookmarkPlus" 
                                         Width="32" 
                                         Height="32"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                         VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                <TextBlock Text="Add Bookmark"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Path (read-only) -->
            <TextBox Grid.Row="1"
                     x:Name="PathTextBox"
                     materialDesign:HintAssist.Hint="Path"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     IsReadOnly="True"
                     Margin="0,0,0,16">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- Name (editable) -->
            <TextBox Grid.Row="2"
                     x:Name="NameTextBox"
                     materialDesign:HintAssist.Hint="Bookmark Name"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Margin="0,0,0,24">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ElementName=SaveButton, Path=Command}"/>
                    <KeyBinding Key="Escape" Command="{Binding ElementName=CancelButton, Path=Command}"/>
                </TextBox.InputBindings>
            </TextBox>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="CANCEL"
                        IsCancel="True"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="SaveButton"
                        Content="ADD BOOKMARK"
                        IsDefault="True"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
