<Window x:Class="EasyFileManager.WPF.Views.CreateArchiveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Create Archive" 
        SizeToContent="Height"
        Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}">
    <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}"
            BorderThickness="2"
            CornerRadius="4">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="FolderZip" 
                                         Width="32" 
                                         Height="32"
                                         VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                <TextBlock Text="Create Archive"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Archive Info -->
            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                <TextBlock Text="Archive Name:"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Margin="0,0,0,4"/>
                <TextBox x:Name="ArchiveNameTextBox"
                         materialDesign:HintAssist.Hint="Enter archive name..."
                         Style="{StaticResource MaterialDesignFilledTextBox}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ElementName=CreateButton, Path=Command}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <TextBlock Text="{Binding ItemCountText}"
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                           Opacity="0.7"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Options -->
            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                <TextBlock Text="Options:"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <ComboBox x:Name="CompressionLevelComboBox"
                          materialDesign:HintAssist.Hint="Compression Level"
                          Style="{StaticResource MaterialDesignFilledComboBox}"
                          SelectedIndex="2"
                          Margin="0,0,0,8">
                    <ComboBoxItem Content="Store (No Compression)" Tag="Store"/>
                    <ComboBoxItem Content="Fastest" Tag="Fastest"/>
                    <ComboBoxItem Content="Default (Recommended)" Tag="Default"/>
                    <ComboBoxItem Content="Best Compression" Tag="Best"/>
                </ComboBox>

                <!-- Password section commented out - not supported yet
                <TextBox materialDesign:HintAssist.Hint="Password (Optional)"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         Margin="0,0,0,8"
                         x:Name="PasswordTextBox"/>
                -->
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="CANCEL"
                        Click="CancelButton_Click"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0"/>
                <Button x:Name="CreateButton"
                        Content="CREATE"
                        Click="CreateButton_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>